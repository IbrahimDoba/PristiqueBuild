import React from 'react'
import { SliderPropsType } from './SliderData'

const SliderComp = ({ image, title, text }: SliderPropsType) => {
  return (
    <div>
      
    </div>
  )
}

export default SliderComp
